<view class="page coach"><view class="intro card"><view class="title small">饮食记录回顾</view><view class="subtitle">仅整理你已经确认的记录，不会自动修改饮食数据，也不会代替产检建议。</view></view><view class="chips"><button wx:for="{{prompts}}" wx:key="period" data-period="{{item.period}}" loading="{{loading}}" disabled="{{loading}}" bindtap="generate">{{item.text}}</button></view><view wx:if="{{draft}}" class="draft card"><view>本周期回顾</view><text>{{draftText}}</text><small>模型 {{draft.model_name}} · 提示版本 {{draft.prompt_version}}</small><button data-period="{{currentPeriod}}" loading="{{loading}}" disabled="{{loading}}" bindtap="generate">重新生成 {{currentPeriod}} 天回顾</button></view><view class="safety">AI 只接收周期、记录天数和食物类别等汇总事实。出现明显不适、疾病或用药问题时，请联系医生。</view></view>
