const {getCurrentPregnancy}=require("../../api/pregnancy");const {createInvitation,listMembers,updatePermissions,revokeMember,listTasks,createTask,updateTask}=require("../../api/family");const {localDateKey}=require("../../utils/date");
Page({data:{mode:"owner",pregnancy:null,members:[],tasks:[],invitationToken:"",loading:true},onShow(){this.load();},async load(){this.setData({loading:true});try{const memberships=await listMembers();let pregnancy=null,mode="partner",subjectUserId="";try{pregnancy=await getCurrentPregnancy();mode="owner";subjectUserId=pregnancy.user_id;}catch(error){const own=(memberships.items||[]).find(item=>item.status==="active");if(own)subjectUserId=own.owner_user_id;}const members=(memberships.items||[]).map(item=>({...item,canMeal:item.permission_scopes.indexOf("meal_entry:write_for_owner")>=0,canTask:item.permission_scopes.indexOf("family_task:write")>=0}));const tasks=subjectUserId?(await listTasks(localDateKey(),mode==="partner"?subjectUserId:"")).items||[]:[];this.setData({mode,pregnancy,members,tasks,loading:false});}catch(error){console.warn("family",error);this.setData({loading:false});}},async invite(){try{const result=await createInvitation();this.setData({invitationToken:result.token});wx.setClipboardData({data:result.token,title:"邀请码已复制"});}catch(error){wx.showToast({title:"暂时无法创建邀请",icon:"none"});}},async toggleMeal(e){const member=e.currentTarget.dataset.member,scopes=member.permission_scopes.filter(item=>item!=="meal_entry:write_for_owner");if(!member.canMeal)scopes.push("meal_entry:write_for_owner");try{await updatePermissions(member.id,scopes);this.load();}catch(error){wx.showToast({title:"权限保存失败",icon:"none"});}},revoke(e){const id=e.currentTarget.dataset.id;wx.showModal({title:"撤销家属权限",content:"撤销后将立即无法查看或代记。",success:async result=>{if(result.confirm){await revokeMember(id);this.load();}}});},addTask(){wx.showModal({title:"添加家庭任务",editable:true,placeholderText:"例如：准备晚餐食材",success:async result=>{if(!result.confirm||!result.content.trim())return;const member=this.data.members.find(item=>item.status==="active");try{await createTask({task_date:localDateKey(),task_type:"other",title:result.content.trim(),assignee_user_id:member&&member.member_user_id});this.load();}catch(error){wx.showToast({title:"任务保存失败",icon:"none"});}}});},async completeTask(e){try{await updateTask(e.currentTarget.dataset.id,{status:"completed"});this.load();}catch(error){wx.showToast({title:"暂无完成权限",icon:"none"});}}});
