<view class="page stack"><view class="title">拍下这一餐</view><view class="subtitle">AI 只会给出食物候选和估算克数。你确认后，营养值才会从 TKA 数据中计算并写入。</view><image wx:if="{{imagePath}}" class="preview" mode="aspectFill" src="{{imagePath}}"/><button class="button-light" bindtap="choose">{{imagePath?'重新选择':'选择照片'}}</button><button wx:if="{{imagePath}}" class="button-primary" loading="{{uploading}}" bindtap="upload">上传并生成候选</button><view wx:if="{{uploaded}}" class="card notice">照片已安全上传。识别服务未配置时不会生成虚假的食物结果，请改用食物搜索确认。</view></view>
